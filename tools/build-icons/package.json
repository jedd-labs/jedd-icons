{
  "name": "@jedd-icons/build-icons",
  "version": "0.0.1",
  "description": "Build tooling that generates icon source files from SVGs.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
